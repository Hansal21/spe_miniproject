---
- name: Pull docker image from dockerhub
  hosts: all
  tasks:
    - name: Pull docker image
      docker_image:
        name: hansal21/miniproject
        source: pull
    - name: starting container
      shell: docker start --name mycontainer hansal21/miniproject:latest